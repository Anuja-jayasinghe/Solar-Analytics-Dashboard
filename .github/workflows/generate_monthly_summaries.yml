name: DISABLED — Generate Monthly Summaries (duplicate)

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This duplicate workflow file is disabled; use generate-monthly-summaries.yml"