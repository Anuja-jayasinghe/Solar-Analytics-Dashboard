name: DISABLED — Update Total Generation (duplicate)

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This duplicate workflow file is disabled; use update-total-generation.yml"