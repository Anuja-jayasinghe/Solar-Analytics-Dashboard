name: DISABLED — Fetch SolisCloud Inverter Data (duplicate)

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This duplicate workflow file is disabled; use fetch-inverter-data.yml"